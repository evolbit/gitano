# Features

Feature folders own user-facing Git workflows. A feature can contain its own components, hooks, stores, types, API adapters, utilities, and tests.

Shared code should be promoted to `src/shared` only when multiple features need it or when it is platform/application infrastructure.
