# App Layer

Application bootstrap, providers, shell composition, startup effects, and global orchestration live here.

Feature-owned workflow code belongs in `src/features`. Reusable infrastructure and UI primitives belong in `src/shared`.
