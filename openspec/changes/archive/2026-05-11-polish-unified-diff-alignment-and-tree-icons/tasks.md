## 1. Toolbar and explorer controls

- [x] 1.1 Bind the toolbar repository/branch selector region to the same live left sidebar width state used by the workspace pane.
- [x] 1.2 Add visible flat/tree icon toggle controls to the current changes explorer header and wire them to the existing persisted working-changes view mode.
- [x] 1.3 Add the same visible flat/tree icon toggle controls to the commit changes explorer header and wire them to the existing persisted commit-changes view mode.

## 2. Diff and tree layout polish

- [x] 2.1 Update unified diff rows so line numbers and content top-align using the same vertical rhythm as split rows.
- [x] 2.2 Preserve fixed non-shrinking chevron and folder icon slots in tree rows so narrow commit panes truncate labels before shrinking icons.
- [x] 2.3 Verify the toolbar width coupling, visible view toggles, unified diff alignment, and narrow commit tree icon stability together in the workspace and modal surfaces.
