## 1. Commit box keyboard commit workflow

- [x] 1.1 Implement `Enter` commit behavior in the current changes commit box
- [x] 1.2 Implement `Shift+Enter` commit+push behavior with explicit push override

## 2. Shared push feedback and loading behavior

- [x] 2.1 Route commit-box push operations through the shared push feedback pathway used by toolbar push
- [x] 2.2 Ensure toolbar push loading appears only during explicit push operations, including commit-box initiated push

## 3. Commit history refresh behavior

- [x] 3.1 Trigger immediate commit list refresh after successful commit-box commit actions
- [x] 3.2 Preserve/verify periodic commit-list refresh after post-commit immediate reload
